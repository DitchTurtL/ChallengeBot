@page "/challenges"
@page "/challenges/{id:long}"

<h3>Challenges</h3>

@code {
    [Parameter]
    public long Id { get; set; }

    protected override async Task OnInitializedAsync()
    {
        //htmlContent = await HtmlContentService.GetHtmlContentByIdAsync(Id);
    }

}
